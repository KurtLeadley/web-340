<% include header %>
<% include navbar %>
<div id="container">
  <h1>Employee Records List</h1>
  <% console.log(typeof employees); %>
  <% employees.forEach(function(entry) {
    console.log(entry.firstName+" "+entry.lastName);
  }); %>
  <table>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
    </tr>
    <% employees.forEach(function(entry) { %>
        <tr>
          <td><%=entry.firstName%></td>
          <td><%=entry.lastName%></td>
        </tr>
    <% }); %>
  </table>
</div>
<% include scripts %>
<% include footer %>
